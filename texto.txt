dotnet publish -c Release


git status
git add .
git commit -m "Descrição da alteração"
git push  //Envia para a nuvem do github


git pull //baixa para o seu repositorio local

//Enviar para VPN

scp "D:\OneDrive\Documentos\MeusAPP_git\MinhaAplicacaoBlazor\bin\Release\net10.0\publish.zip" root@2.24.64.158:/var/www/MinhaAplicacaoBlazor/

//Descompactar
unzip arquivo.zip

//Descompactar sobrescrevendo arquivos existentes
unzip -o publish.zip

//remover
rm publish.zip
rm -rf /var/www/MinhaAplicacaoBlazor/*

//Compila e publica para versao de producao
dotnet publish -c Release

//Ver sites ativos no Nginx
ls -la /etc/nginx/sites-enabled/

//Ver configuração de cada site
sudo cat /etc/nginx/sites-available/minhaaplicacao

//Ver para qual porta cada app aponta
sudo grep -R "proxy_pass" /etc/nginx/sites-enabled/

//Desativar uma aplicação no Nginx
//Para tirar uma aplicação do Nginx, remova o link em sites-enabled.
sudo rm /etc/nginx/sites-enabled/blazor 
sudo nginx -t
sudo systemctl reload nginx